substitutions:
  _EXEC_NAME: 'pinger'
  _GITHUB_ORG: 'dillydev'
  _GITHUB_REPO: 'pinger-k8s'
  _COMPUTE_ZONE: 'us-central1-b'
  _CONTAINER_CLUSTER: 'hello-cloudbuild'
  _IMG: ''

steps:
# Copy the files from storage needed to connect to git.
- name: 'gcr.io/cloud-builders/gsutil'
  id: copy ssh keys for github
  args: ['cp', 'gs://pipeline-test-20190604/*', '.']

- name: 'gcr.io/cloud-builders/gcloud'
  id: load github key
  args:
  - kms
  - decrypt
  - --ciphertext-file=id_rsa.enc
  - --plaintext-file=/root/.ssh/id_rsa
  - --location=global
  - --keyring=cloudbuilder
  - --key=github-key
  volumes:
  - name: 'ssh'
    path: /root/.ssh

# Set up git with key and domain.
- name: 'gcr.io/cloud-builders/git'
  id: set ssh key for github
  entrypoint: 'bash'
  args:
  - '-c'
  - |
    chmod 600 /root/.ssh/id_rsa
    cat <<EOF >/root/.ssh/config
    Hostname github.com
    IdentityFile /root/.ssh/id_rsa
    EOF
    mv known_hosts /root/.ssh/known_hosts
  volumes:
  - name: 'ssh'
    path: /root/.ssh

# update helm values 
- name: 'gcr.io/cloud-builders/git'
  id: Update helm values for prod
  entrypoint: /bin/sh
  args:
  - '-c'
  - |
     grep -o 'pinger:.*' manifests/$_EXEC_NAME/templates/deployment.yaml > _IMG && \
     cat _IMG && \
     TESTVAR=$(cat _IMG)
     echo $TESTVAR
     git fetch origin candidate-prod && git checkout candidate-prod && \
     git config --global user.email $(gcloud auth list --filter=status:ACTIVE --format='value(account)') && \
     sed "s/GOOGLE_CLOUD_PROJECT/${PROJECT_ID}/g" values.yaml.tpl | \
     sed "s/APP_IMAGE/$_IMG/g" > $_EXEC_NAME/values.yaml
  volumes:
  - name: 'ssh'
    path: /root/.ssh

- name: 'gcr.io/$PROJECT_ID/helm'
  id: generate manifest
  entrypoint: bash
  args:
  - '-c'
  - |
    helm template --output-dir ./manifests ./$_EXEC_NAME
  env:
  - 'CLOUDSDK_COMPUTE_ZONE=us-central1-b'
  - 'CLOUDSDK_CONTAINER_CLUSTER=hello-cloudbuild'

# push new helm values to k8s repo
- name: 'gcr.io/cloud-builders/git'
  id: commit new helm values
  entrypoint: /bin/sh
  args:
  - '-c'
  - |
    git remote remove origin && git remote add origin git@github.com:$_GITHUB_ORG/$_GITHUB_REPO.git && \
    git add $_EXEC_NAME manifests && \
    git commit -m "Deploying image $_GCR_IMAGE
    Build from image $_GCR_IMAGE of repository $_GITHUB_REPO" && \
    git push origin candidate-prod
  volumes:
  - name: 'ssh'
    path: /root/.ssh